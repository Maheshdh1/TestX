# Frontend – Question Paper Generator (Next.js + Tailwind)

## Setup

```bash
npm install
# Optionally set NEXT_PUBLIC_API_URL in .env.local
npm run dev
```

By default it expects the backend on http://localhost:5000.
